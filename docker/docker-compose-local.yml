services:
  mysql:
    image: mysql:8.0
    container_name: gdg-mysql
    restart: always
    env_file:
      - .env
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7.2.5
    container_name: gdg-redis
    ports:
      - '6379:6379'
